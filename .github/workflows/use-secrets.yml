name: Use Secrets
on:
  push:
    paths:
      - ".github/workflows/use-secrets.yml"

jobs:
  check-secret:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Grade use-secrets activity
        with:
          secrets-context: ${{ toJSON(secrets.EXERCISE_SECRET) }}
        id: check-secrets
        uses: ./.github/actions/check-secrets-action
        
